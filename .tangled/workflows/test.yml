when:
  - event: ["push"]
    branch: ci-test

engine: nixery

dependencies:
  nixpkgs:
    - cargo

steps:
  - name: run linter
    command: |
      nix develop . -c cargo clippy --all-targets
  - name: run all tests
    command: |
      nix develop . -c cargo test

